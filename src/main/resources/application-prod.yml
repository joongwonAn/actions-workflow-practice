server:
  port: 8080

logging:
  level:
    root: INFO
  file:
    name: /app/logs/myapp.log # 저장 위치 설정

management:
  endpoints:
    web:
      exposure:
        include: health

DB_HOST: ${DB_HOST}
DB_PASSWORD: ${DB_PASSWORD}